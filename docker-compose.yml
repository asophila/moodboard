version: '3.8'

services:
  moodboard:
    build: .
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

  # Optional: Include Supabase for full self-hosting
  # Uncomment and configure if you want to self-host the backend
  # supabase:
  #   image: supabase/postgres:15
  #   environment:
  #     POSTGRES_PASSWORD: your_password
  #   volumes:
  #     - supabase-data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# volumes:
#   supabase-data:
